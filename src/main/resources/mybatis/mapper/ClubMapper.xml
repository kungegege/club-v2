<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.srb.club.pojo.mapper.ClubMapper">
    <resultMap id="club" type="ClubEntity">
        <result column="club_name" property="clubName"></result>
        <result column="update_time" property="updateTime"></result>
        <result column="create_time" property="createTime"></result>
        <result column="host_id" property="hostId"></result>
    </resultMap>

    <select id="selectClubByCid" resultMap="club">
        select * from club where cid=#{cid}
    </select>

</mapper>

